import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:mobx_jornada/app/modules/models/client_controller.dart';
import 'package:mobx_jornada/app/app_module.dart';

void main() {
  initModule(AppModule());
  // ClientController client;
  //
  setUp(() {
    //     client = AppModule.to.get<ClientController>();
  });

  group('ClientController Test', () {
    //   test("First Test", () {
    //     expect(client, isInstanceOf<ClientController>());
    //   });

    //   test("Set Value", () {
    //     expect(client.value, equals(0));
    //     client.increment();
    //     expect(client.value, equals(1));
    //   });
  });
}
